# FORT 社内ポータル ｜ プロジェクト憲法

> このファイルは Claude Code が最初に読む中心ドキュメントです。
> 実装を始める前に、必ず `docs/` 以下の設計書も読んでください。

---

## このプロジェクトは何か

株式会社FORT（岡山・福山の注文住宅／建売の工務店）の**社内ポータルサイト**。
現在バラバラのGoogleスプレッドシートで管理している業務（日報・勤怠・席予約など）を、
ひとつのWebアプリにまとめてFORT全社員が日常的に使う土台をつくる。

これは「見た目のプロトタイプ」ではなく、**実際に運用する本番システム**である。
データはデータベースに保存され、社員がログインして毎日使う。

---

## 絶対に守る前提（技術スタック）

| 項目 | 採用技術 | 備考 |
|------|----------|------|
| フレームワーク | **Next.js 15**（App Router） | React ベース。フルスタック（画面＋API）を1つで作れる |
| 言語 | **TypeScript** | 型で安全に |
| データベース | **Supabase**（PostgreSQL） | 認証・DB・ストレージが揃っている |
| 認証 | **Supabase Auth ＋ Google OAuth** | @fort410.jp のGoogleアカウントでログイン |
| スタイリング | **Tailwind CSS** | |
| UIコンポーネント | **shadcn/ui** | Tailwindベースの部品集 |
| ホスティング | **Vercel** | Next.jsと相性が良い。GitHub連携で自動デプロイ |
| アイコン | **lucide-react** | |

この構成は確定事項。勝手に別のフレームワークやDBに変更しないこと。

---

## デザインの決めごと（FORTブランド）

FORTは「ミニマル・建築的・editorial」な美意識を大切にしている。
装飾過多を避け、余白・タイポグラフィで見せる。以下のトークンを厳守：

```
背景        #F4F3EF （温かみのある オフホワイト）
カード      #FFFFFF
文字        #171717
サブ文字    #777773
罫線        #D9D8D2
ダーク面    #242424 （サイドバー等）
アクセント  ブロンズ #9A8060 / オリーブ #6F7565
英字見出し  Inter
和文        Noto Sans JP / Zen Kaku Gothic New
```

- 青系の色は使わない（FORTらしくない）。
- 状態色：正常=オリーブ系、注意=サンド/ゴールド、遅延=テラコッタ、緊急=くすんだ赤。
- Appleのような静けさ。角丸は控えめ（6〜8px）。影は極薄。

詳細は `docs/03_design_system.md` を参照。

---

## 開発の進め方（超重要）

**「思いつく限り全部」載せたいが、全部を一度に作らない。**
全体像（フルスコープ）は `docs/02_features.md` に描いてあるが、
実装は **Phase を区切って順番に** 進める。各Phaseが動く状態になってから次へ。

開発ロードマップは `docs/06_roadmap.md` にある。**必ず Phase 0 → 1 → 2… の順で進める。**
勝手に先のPhaseに飛ばない。各Phase完了時に動作確認できる状態にする。

---

## ドキュメント一覧（docs/）

| ファイル | 内容 |
|----------|------|
| `01_overview.md` | プロジェクト全体像・背景・ゴール |
| `02_features.md` | 全機能一覧（フルスコープ）とPhase割り当て |
| `03_design_system.md` | 色・フォント・UIルールの詳細 |
| `04_data_model.md` | データベース設計（全テーブル定義） |
| `05_architecture.md` | 技術構成・フォルダ構成・認証の仕組み |
| `06_roadmap.md` | 開発フェーズと着手順（**ここから実装開始**） |
| `07_setup.md` | 環境構築手順（Supabase・Google OAuth・Vercel） |

---

## 実装時の心得

- **小さく作って動かす。** 1機能ずつ、動く状態を確認しながら進める。
- **既存データを尊重する。** 実データの構造は `docs/04_data_model.md` に反映済み。
- **セキュリティ。** Supabase の RLS（Row Level Security）で、社員は自分に関係するデータだけ見えるようにする。管理職・経営は広く見える。
- **わからないことは勝手に決めない。** 仕様に迷ったら、選択肢を提示して確認を求める。
- **日本語UI。** 画面の文言はすべて日本語。コード内のコメントも日本語でよい。

---

## FORTの組織・実データについて

- 社員は約14名（経営・営業・設計・工務・総務）。岡山本社と福山スタジオの2拠点。
- ログインユーザーの想定代表：青野 弘輝（専務取締役）。
- 会計年度が特殊：10期〜2026年9月、11期は短期4ヶ月（2026年10月〜2027年1月、M&Aのため）、12期〜2027年2月。
- 実データのサンプル（社員・案件・日報の構造）は `docs/04_data_model.md` の末尾にある。
