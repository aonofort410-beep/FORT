import { Statement } from "@/components/ui/Statement";

export function Concept() {
  return (
    <Statement supporting="FORTが選ぶ理由には、いつも同じ基準があります。">
      標準仕様は、暮らしを整えるための基準です。
    </Statement>
  );
}
